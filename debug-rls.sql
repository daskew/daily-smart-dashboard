-- Disable RLS for debugging (re-enable after testing)
ALTER TABLE public.connected_accounts DISABLE ROW LEVEL SECURITY;

-- Also ensure the table has data
-- Run this to check if accounts are being saved
-- SELECT * FROM public.connected_accounts;
